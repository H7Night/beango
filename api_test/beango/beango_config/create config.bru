meta {
  name: create config
  type: http
  seq: 2
}

post {
  url: http://127.0.0.1:10777/beango_config/create
  body: json
  auth: inherit
}

body:json {
  {
    "config_key": "outputFolder",
    "config_value": "./output",
    "note":"outputFolder"
  }
}
